import React from 'react';
import Swap from '../components/Swap';

const Home = () => {
  return (
    <div className="d-flex justify-content-center">
      <Swap />
    </div>
  );
};

export default Home;
